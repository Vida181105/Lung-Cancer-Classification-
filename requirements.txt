# Pinned loosely: Kaggle's GPU image already ships compatible versions of all of
# these, so the notebooks run there with no pip install at all. These bounds are
# for reproducing the environment locally.

tensorflow>=2.15,<2.20
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7
pillow>=9.5

# Notebook tooling (not needed on Kaggle, which provides its own).
jupyter>=1.0
notebook>=7.0
